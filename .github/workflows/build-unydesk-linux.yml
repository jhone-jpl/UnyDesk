# Deprecated as standalone entrypoint — use "Build UnyDesk Clients".
# Kept so old bookmarks still resolve; redirects via workflow_call is removed.
# Prefer: .github/workflows/build-unydesk-clients.yml

name: Build UnyDesk Linux (legacy)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-22.04
    steps:
      - name: Use combined workflow
        run: |
          echo "::error::Use Actions → 'Build UnyDesk Clients' instead of this legacy workflow."
          exit 1
